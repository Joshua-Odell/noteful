//notes generated into a list then passed into the list component
// Each list has a title, date modified and a delete button

import React from 'react';

export default function Note() {
    return(
        <li>Note #1</li>
    )
}